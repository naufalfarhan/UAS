<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="polinema.ac.id.UAS">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="UTS"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="polinema.ac.id.UAS.activities.BundleActivity"></activity>
        <activity android:name="polinema.ac.id.UAS.activities.WelcomeActivity" />
        <activity android:name="polinema.ac.id.UAS.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="polinema.ac.id.UAS.activities.MainActivity"></activity>
    </application>

</manifest>